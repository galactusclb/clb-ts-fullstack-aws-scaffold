model User {
    id           String   @id @default(cuid())
    email        String   @unique
    passwordHash String?
    googleId     String?  @unique
    role         Role     @default(USER)
    posts        Post[]
    createdAt    DateTime @default(now())
}

enum Role {
    USER
    ADMIN
}